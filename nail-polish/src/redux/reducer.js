import { POLISH } from '../shared/polish';
import { GIFTCARDS } from '../shared/giftcards';

export const initialState = {
    polish: POLISH,
    giftcards: GIFTCARDS
};

export const Reducer = (state = initialState, action) => {
    return state;
};